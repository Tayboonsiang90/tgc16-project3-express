{{#extends "base"}}

    {{#block "content"}}

        <div>
            <form method="POST">
              <a href="/arts/{{artsId}}/owners/" class="btn btn-lg btn-secondary">Back</a>
                <h1 class="mt-3">Add an Owner</h1>
                <input type="hidden" name="_csrf" value="{{csrfToken}}" />
                {{{form}}}
                <input type="submit" value="Add Owner" class="btn btn-primary mt-2" />
            </form>
        </div>

    {{/block}}

{{/extends}}