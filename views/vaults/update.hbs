{{#extends "base"}}

    {{#block "content"}}
        <h1>Update Vault: {{tag.name}}</h1>
        <form method="POST">
            <input type="hidden" name="_csrf" value="{{csrfToken}}" />
            {{{form}}}
            <input type="submit" value="Update Vault" class="btn btn-primary mt-3" />
        </form>
    {{/block}}

{{/extends}}